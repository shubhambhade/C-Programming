#include <stdio.h>

int main() {
    int num;

    // Prompt the user for input
    printf("Enter value of number: ");

    // Taking input from user
    scanf("%d", &num);

    // Print the output
    printf("Value of number: %d\n", num);

    return 0;
}
